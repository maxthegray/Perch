import SwiftUI

/// SwiftUI view for a single stored item (icon + title).
struct ItemRowView: View {
    let item: StoredItem

    var body: some View {
        // TODO(T4): render `item.iconImage()` + `item.metadata.title`.
        EmptyView()
    }
}
